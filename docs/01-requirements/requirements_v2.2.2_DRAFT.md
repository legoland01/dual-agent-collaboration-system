# 需求规格说明书：oc-collab v2.2.2

**版本**: v1
**创建日期**: 2026-02-07
**作者**: Agent 1 (产品经理)
**版本号**: 2.2.2
**状态**: DRAFT (草稿) → 待 Agent 2 评审

---

## 1. 概述

### 1.1 版本信息

| 项目 | 值 |
|------|-----|
| 当前版本 | v2.2.1 |
| 上一版本 | v2.2.1.post1 |
| 变更类型 | 自动化增强 + 协作机制改进 |

### 1.2 变更动机

v2.2.1 发布后，发现以下问题需要解决：

| 问题来源 | 问题描述 | 严重程度 | 解决方案 |
|----------|----------|----------|----------|
| Agent2 反思 | P9: 评审流于形式，只"确认"不"质疑" | P1 | 动态评审 Checklist + 逆向验证 |
| Agent1 反思 | 不知道如何发布到 PyPI | P1 | 部署发布自动化 |
| 协作实践 | todowrite 只更新内存不同步 | P1 | 任务状态自动同步 |
| 协作实践 | signoff.py 覆盖率仅 69% | P2 | 测试覆盖率门禁 |
| 协作实践 | 文档版本混乱 | P2 | 文档版本管理 |
| 协作实践 | Session 重启需重新解释 oc-collab 规则 | P1 | Agent 身份自动识别 |

### 1.3 主要变更

1. **动态评审 Checklist**: 引入逆向验证机制，解决评审流于形式问题
2. **部署发布自动化**: 支持 deployment.yaml 配置和自动发布
3. **任务状态自动同步**: todowrite 自动同步到文件
4. **测试覆盖率门禁**: 新代码提交强制检查覆盖率 ≥ 80%
5. **文档版本管理**: 自动管理文档版本，避免混乱
6. **Agent 身份自动识别**: Session 启动自动加载对应规则和欢迎消息

---

## 2. 功能需求

### 2.1 F-REVIEW-001: 动态评审 Checklist（含逆向验证机制）

**需求编号**: FR-REVIEW-001

**核心洞察（Agent2 发现）**:
P9 问题的本质是"上下文混同"而非能力问题。Agent2 与 Agent1 共享过多上下文，失去了独立评审视角。解决方案不是简单的 checklist，而是**逆向验证机制**：强制 Agent 从"批判"角度审视文档，而非"确认"角度。

#### 2.1.1 逆向检查项

**描述**: 评审时自动生成逆向检查项，强制 Agent 从批判角度审视文档。

| 序号 | 逆向检查项 | 目的 |
|------|-----------|------|
| REV-001 | "这份文档是否忽略了什么重要因素？" | 强制寻找遗漏 |
| REV-002 | "如果你是另一个角色，你会怎么批评这个设计？" | 强制换位思考 |
| REV-003 | "这个方案的最大风险是什么？如何缓解？" | 强制风险评估 |
| REV-004 | "有哪些替代方案？为什么当前方案是最好的？" | 强制对比分析 |
| REV-005 | "这个实现细节是否经得起推敲？" | 强制细节审视 |

#### 2.1.2 正向检查项

**描述**: 评审时自动生成正向检查项，确保基本质量。

| 序号 | 正向检查项 | 目的 |
|------|-----------|------|
| POS-001 | 功能完整性 | 确保覆盖所有需求 |
| POS-002 | 技术可行性 | 确保技术方案可行 |
| POS-003 | 文档规范性 | 确保格式规范 |

#### 2.1.3 验收标准

- [ ] 评审需求文档时，自动生成逆向检查项（≥50%）和正向检查项
- [ ] 评审设计文档时，自动生成逆向检查项（≥50%）和正向检查项
- [ ] 强制 Agent 回答所有逆向检查项
- [ ] 校验逆向检查项回答质量（必须有实质性质疑内容）
- [ ] 正向检查项作为补充，确保基本质量

---

### 2.2 F-AUTO-001: 部署发布自动化

**需求编号**: FR-AUTO-001

#### 2.2.1 deployment.yaml 配置

**描述**: 支持在项目根目录创建 deployment.yaml 配置文件。

**配置格式**:
```yaml
deployment:
  type: python_pypi
  commands:
    build: python3 -m build
    upload: python3 -m twine upload dist/*
  conditions:
    - phase: deployment
```

#### 2.2.2 交互式配置

**描述**: 支持 `oc-collab deployment configure` 交互式配置。

**命令格式**:
```bash
oc-collab deployment configure
```

**交互流程**:
1. 选择部署类型（PyPI / Docker / NPM / Custom）
2. 输入构建命令
3. 输入上传/发布命令
4. 确认配置

#### 2.2.3 自动发布

**描述**: `oc-collab phase-advance` 时自动执行发布命令。

**命令格式**:
```bash
oc-collab phase-advance --deploy
```

#### 2.2.4 验收标准

- [ ] 支持 `deployment.yaml` 配置文件
- [ ] 支持 `oc-collab deployment configure` 交互式配置
- [ ] `oc-collab phase-advance` 时自动执行发布命令（需 --deploy 选项）
- [ ] 发布前显示预览，确认后执行

---

### 2.3 F-AUTO-002: 任务状态自动同步

**需求编号**: FR-AUTO-002

#### 2.3.1 自动文件同步

**描述**: todowrite / todoedit 操作后自动同步到文件。

**命令格式**:
```bash
oc-collab todo add "新任务" --agent agent1
oc-collab todo done 1
oc-collab todo edit 1 --status in_progress
```

**行为**:
1. 执行 todowrite / todoedit 操作
2. 自动更新 state/agent_adhoc_todos.yaml
3. 显示同步确认

#### 2.3.2 验收标准

- [ ] `todowrite` / `todoedit` 操作后自动同步到文件
- [ ] 同步时有明确提示（"✓ 已同步到文件"）
- [ ] 支持查看同步历史

---

### 2.4 F-AUTO-003: 测试覆盖率门禁

**需求编号**: FR-AUTO-003

#### 2.4.1 覆盖率检查

**描述**: 新代码提交时，强制检查单元测试覆盖率不低于阈值。

**命令格式**:
```bash
oc-collab check coverage --threshold 80
```

**行为**:
1. 运行单元测试
2. 计算覆盖率
3. 对比阈值
4. 不达标时阻止合并

#### 2.4.2 验收标准

- [ ] 新代码的单元测试覆盖率 ≥ 80%
- [ ] 覆盖率不达标时阻止合并
- [ ] 支持自定义阈值
- [ ] 显示覆盖率报告

---

### 2.5 F-AUTO-004: 文档版本管理

**需求编号**: FR-AUTO-004

#### 2.5.1 版本标记

**描述**: 新版本发布后，旧版本自动标记为过期。

**行为**:
1. 检测到新版本文档
2. 自动将旧版本标记为过期
3. 更新版本索引

#### 2.5.2 版本冲突检测

**描述**: 避免同一阶段存在多个版本。

**行为**:
1. 检测同一阶段多个版本
2. 提示用户选择有效版本
3. 其他版本自动标记为草稿

#### 2.5.3 验收标准

- [ ] 新版本发布后，旧版本自动标记为过期
- [ ] 避免同一阶段存在多个版本
- [ ] 提供版本索引查询

---

### 2.6 F-IDENTITY-001: Agent 身份自动识别与 Session 启动引导

**需求编号**: FR-IDENTITY-001

#### 2.6.1 身份识别

**描述**: 新 Session 启动时，自动识别 Agent 身份并加载对应规则。

**触发条件**:
- 环境变量 `OC_AGENT_ID=agent1`
- 环境变量 `OC_AGENT_ID=agent2`

**行为**:
1. 读取环境变量 `OC_AGENT_ID`
2. 加载对应的 skill 文件
3. 应用对应的规则

#### 2.6.2 欢迎消息

**描述**: Agent 进入项目时，自动显示欢迎消息。

**消息内容**:
- 项目名称
- 当前阶段
- 版本
- Agent 角色
- 待办任务
- 下一步建议

**命令格式**:
```bash
oc-collab start
```

**输出示例**:
```
=== 欢迎使用 OC-Collab ===

项目: oc-collab
当前阶段: development
版本: v2.2.2

你的角色: Agent 2

待办任务 (2 项):
1. [P0] 实现 F-REVIEW-001 - pending
2. [P1] 实现 F-AUTO-001 - pending

下一步: oc-collab todo 查看详情
```

#### 2.6.3 协作指南摘要

**描述**: 显示协作指南摘要，提供完整指南入口。

**内容**:
- 角色职责
- 工作流程
- 常用命令

#### 2.6.4 验收标准

- [ ] 环境变量 `OC_AGENT_ID` 正确识别 Agent 身份
- [ ] 自动加载对应 skill 文件
- [ ] `oc-collab start` 命令可用
- [ ] 欢迎消息包含：项目名称、阶段、版本、角色、待办、下一步
- [ ] 显示协作指南摘要，提供完整指南入口

---

## 3. 非功能需求

### 3.1 性能需求

| 需求项 | 要求 |
|--------|------|
| todowrite 同步延迟 | ≤ 1 秒 |
| 覆盖率检查时间 | ≤ 30 秒 |
| 欢迎消息生成时间 | ≤ 2 秒 |

### 3.2 兼容性需求

| 需求项 | 要求 |
|--------|------|
| Python 版本 | ≥ 3.8 |
| 操作系统 | macOS / Linux |
| 依赖包 | 与 v2.2.1 兼容 |

### 3.3 安全需求

| 需求项 | 要求 |
|--------|------|
| 环境变量安全 | 不在日志中暴露敏感信息 |
| 部署凭据 | 通过配置文件管理，不硬编码 |

---

## 4. 验收标准汇总

| 功能 | 验收项 | 状态 |
|------|--------|------|
| F-REVIEW-001 | 逆向检查项 ≥ 50% | ⏳ |
| F-REVIEW-001 | 强制 Agent 回答所有逆向检查项 | ⏳ |
| F-AUTO-001 | deployment.yaml 配置 | ⏳ |
| F-AUTO-001 | oc-collab deployment configure | ⏳ |
| F-AUTO-001 | phase-advance 时自动发布 | ⏳ |
| F-AUTO-002 | todowrite 自动同步到文件 | ⏳ |
| F-AUTO-003 | 覆盖率 ≥ 80% | ⏳ |
| F-AUTO-003 | 不达标时阻止合并 | ⏳ |
| F-AUTO-004 | 旧版本自动标记过期 | ⏳ |
| F-AUTO-004 | 避免同一阶段多版本 | ⏳ |
| F-IDENTITY-001 | OC_AGENT_ID 身份识别 | ⏳ |
| F-IDENTITY-001 | oc-collab start 命令 | ⏳ |
| F-IDENTITY-001 | 欢迎消息包含必要信息 | ⏳ |

---

## 5. 依赖关系

```
F-REVIEW-001: 动态评审 Checklist
    └── 复用 M4 ExtendedChecklistGenerator 架构

F-AUTO-001: 部署发布自动化
    └── 独立功能

F-AUTO-002: 任务状态自动同步
    └── todowrite 工具改造

F-AUTO-003: 测试覆盖率门禁
    └── CI/CD 流水线集成

F-AUTO-004: 文档版本管理
    └── 独立功能

F-IDENTITY-001: Agent 身份自动识别
    └── SessionStarter 扩展
```

---

## 6. 工时预估

| 功能 | 任务 | 工时 |
|------|------|------|
| F-REVIEW-001 | 逆向检查项生成器 | 4h |
| F-REVIEW-001 | 与 ExtendedChecklistGenerator 集成 | 2h |
| F-REVIEW-001 | 单元测试 | 2h |
| F-AUTO-001 | deployment.yaml 解析器 | 2h |
| F-AUTO-001 | 交互式配置 CLI | 3h |
| F-AUTO-001 | 自动发布集成 | 2h |
| F-AUTO-001 | 单元测试 | 2h |
| F-AUTO-002 | todowrite 改造 | 3h |
| F-AUTO-002 | 单元测试 | 2h |
| F-AUTO-003 | 覆盖率检查器 | 2h |
| F-AUTO-003 | CI/CD 集成 | 2h |
| F-AUTO-003 | 单元测试 | 2h |
| F-AUTO-004 | 版本管理器 | 3h |
| F-AUTO-004 | 单元测试 | 2h |
| F-IDENTITY-001 | 身份识别器 | 2h |
| F-IDENTITY-001 | SessionStarter 扩展 | 3h |
| F-IDENTITY-001 | oc-collab start 命令 | 2h |
| F-IDENTITY-001 | 单元测试 | 2h |
| | **合计** | **40h** |

---

## 7. 风险分析

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| F-REVIEW-001 逆向检查项效果不确定 | 中 | 中 | 先在 v2.2.2 试点收集反馈 |
| F-AUTO-003 CI/CD 集成复杂 | 低 | 高 | 参考 v2.1.0 GitHub Actions 配置 |
| F-IDENTITY-001 依赖环境变量配置 | 低 | 中 | 提供启动脚本模板 |

---

## 8. 评审说明

### 8.1 Agent 2 需评审的功能

| 功能 | 评审重点 |
|------|----------|
| F-REVIEW-001 | 逆向检查项设计是否合理？回答质量校验是否可行？ |
| F-AUTO-001 | deployment.yaml 方案是否可行？ |
| F-AUTO-002 | todowrite 改造是否有风险？ |
| F-AUTO-003 | CI/CD 集成配置是否正确？ |
| F-IDENTITY-001 | SessionStarter 扩展复杂度？ |

### 8.2 签署

#### Agent 1 签署

| 角色 | 姓名 | 日期 | 确认 |
|------|------|------|------|
| 产品负责人 | Agent 1 | 2026-02-07 | ✅ |

#### Agent 2 技术评审

| 角色 | 姓名 | 日期 | 确认 |
|------|------|------|------|
| 开发负责人 | Agent 2 | | pending |

---

**文档版本**: v1
**创建日期**: 2026-02-07
**状态**: DRAFT → 待 Agent 2 评审
