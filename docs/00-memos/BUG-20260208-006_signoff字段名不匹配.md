# Bug 报告：signoff.py字段名不匹配（BUG-20260208-004残留）

**Bug ID**: BUG-20260208-006
**严重程度**: P0
**状态**: 待修复（未完全修复）
**发现人**: Agent 1
**发现日期**: 2026-02-08

---

## Bug描述

### 表现形式

| 场景 | 问题 |
|------|------|
| 执行 `oc-collab signoff test` | 报错"当前阶段状态不允许签署" |
| 调试 signoff._get_stage_data('test', state) | 返回空 {} |
| v2.2.4.testing | 有数据但未被识别 |

### 第一阶段修复（已修复）

_get_stage_data() 现在能正确读取 v2.2.4.testing：
```python
if stage == "test":
    stage_data = version_data.get("testing", version_data.get("test", {}))
```

### 第二阶段问题（待修复）

**新问题**：_save_stage_data() 没有保存到 v2.2.4.testing

| 方法 | 读取 | 写入 |
|------|------|------|
| _get_stage_data | v2.2.4.testing ✅ | - |
| _save_stage_data | - | 顶层test ❌ |

### 问题代码

```python
# _save_stage_data() 没有保存到v2.2.x.testing
def _save_stage_data(self, stage: str, state: dict, stage_data: dict):
    ...
    state[status_field] = stage_data  # 保存到顶层test，不是v2.2.4.testing
```

### 影响范围

| 影响 | 严重程度 |
|------|----------|
| can_sign 返回 True ✅ | 已修复 |
| sign() 签署后数据未持久化 ❌ | P0 - 阻塞验收 |

---

## 解决方案（第二阶段）

### 问题：_save_stage_data() 没有保存到v2.2.x.testing

```python
def _save_stage_data(self, stage: str, state: dict, stage_data: dict):
    """保存阶段数据。"""
    ...
    # 当前实现：保存到顶层state["test"]
    state[status_field] = stage_data
    
    # 应该改为：
    # 对于test阶段，保存到v2.2.x.testing
    if stage == "test":
        version_keys = [k for k in state.keys() if re.match(r'^v2\.\d+', k)]
        if version_keys:
            latest_version = sorted(version_keys, ...)[-1]
            version_data = state.get(latest_version, {})
            if "testing" in version_data:
                version_data["testing"] = stage_data
                self.state_manager.save_state(state)
                return
    state[status_field] = stage_data
```

### 测试验证

```bash
# 1. 签署测试
oc-collab signoff test -m "测试签署"

# 2. 验证数据保存
python3 -c "
import yaml
with open('state/project_state.yaml') as f:
    state = yaml.safe_load(f)
print(f\"v2.2.4.testing: {state['v2.2.4']['testing']}\")
# 应该看到：{'agent1_signoff': True, ...}
"
```

---

## 时间线

| 日期 | 事件 |
|------|------|
| 2026-02-08 | Agent 1 发现_get_stage_data问题，创建Bug报告 |
| 2026-02-08 | Agent 2 修复_get_stage_data() |
| 2026-02-08 | Agent 1 发现_save_stage_data问题，更新Bug报告 |
| 2026-02-08 | Agent 2 修复_save_stage_data()（待完成） |

---

## 关联文档

| 文档 | 说明 |
|------|------|
| `src/core/signoff.py` | signoff模块代码 |
| `state/project_state.yaml` | 项目状态文件 |

---

**创建人**: Agent 1
**日期**: 2026-02-08
**状态**: 待修复（_save_stage_data未完全修复）
