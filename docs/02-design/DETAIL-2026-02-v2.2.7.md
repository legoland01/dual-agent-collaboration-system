# 详细设计说明书：oc-collab v2.2.7

**版本**: v1
**创建日期**: 2026-02-10
**作者**: Agent 2 (开发负责人)
**关联需求**: docs/01-requirements/requirements_v2.2.7.md
**关联概要设计**: docs/02-design/OUTLINE_v2.2.7.md
**版本号**: v2.2.7
**状态**: DRAFT → READY

---

## 1. 功能模块映射

### 1.1 映射表

| 功能模块 (概要设计) | 技术模块 (详细设计) | 对应文件 | 工时 |
|---------------------|---------------------|----------|------|
| **F-TEST: Skill保障体系** | | | **12h** |
| ├─ F-TEST-001 | SkillTester | core/skill_tester.py | 2h |
| ├─ F-TEST-001 | ReferenceValidator | core/reference_validator.py | 2h |
| ├─ F-TEST-001 | CLIActionValidator | core/cli_action_validator.py | 2h |
| └─ F-TEST-002 | CoverageCalculator | core/coverage_calculator.py | 4h |
| **F-WEB: Webhook基础设施** | | | **10h** |
| ├─ F-WEB-001 | WebhookConfig | core/webhook_config.py | 4h |
| └─ F-WEB-002 | EventListener (+崩溃恢复) | core/event_listener.py | 4h |
| **F-WEB-003/004 → v2.2.8** | | | **10h** |
| └─ F-WEB-003/004 | EventDispatcher, StateNotifier | core/event_dispatcher.py, core/state_notifier.py | 10h |

### 1.2 新增/变更文件

| 文件路径 | 功能 | 工时 | 类型 |
|----------|------|------|------|
| `src/core/skill_tester.py` | Skill内容准确性测试 | 2h | 新增 |
| `src/core/reference_validator.py` | 引用关系验证 | 2h | 新增 |
| `src/core/cli_action_validator.py` | CLI命令验证 | 2h | 新增 |
| `src/core/coverage_calculator.py` | 覆盖率统计 | 4h | 新增 |
| `src/core/webhook_config.py` | Webhook配置管理 | 4h | 新增 |
| `src/core/event_listener.py` | 事件监听+崩溃恢复 | 4h | 新增 |
| `src/core/event_dispatcher.py` | 事件分发 | 3h | v2.2.8 |
| `src/core/state_notifier.py` | 状态通知 | 3h | v2.2.8 |
| `src/cli/skill_commands.py` | skill test命令 | 1h | 增强 |
| `src/cli/webhook_commands.py` | webhook命令组 | 2h | 新增 |

---

## 2. 技术架构

### 2.1 模块架构图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        oc-collab v2.2.7 质量保障与通知架构                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────────┐ │
│  │                     CLI 命令层 (v2.2.7 新增)                                    │ │
│  ├─────────────────────────────────────────────────────────────────────────────┤ │
│  │  Skill保障命令                      │ Webhook命令                            │ │
│  │  ├─ oc-collab skill test           │ ├─ oc-collab webhook init             │ │
│  │  ├─ oc-collab skill test --skill   │ ├─ oc-collab webhook status           │ │
│  │  ├─ oc-collab skill coverage       │ ├─ oc-collab webhook start             │ │
│  │  └─ oc-collab skill coverage --fix │ └─ oc-collab webhook stop             │ │
│  └─────────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                              │
│                                    ▼                                              │
│  ┌─────────────────────────────────────────────────────────────────────────────┐ │
│  │                         核心功能模块                                           │ │
│  │                                                                              │ │
│  │  ┌─────────────────────────────────────────────────────────────────────────┐│ │
│  │  │  【v2.2.7新增】Skill测试模块                                             ││ │
│  │  │  ├─ SkillTester: 内容准确性验证 [v2.2.7]                                ││ │
│  │  │  ├─ ReferenceValidator: 引用关系验证 [v2.2.7]                           ││ │
│  │  │  ├─ CLIActionValidator: CLI命令验证 [v2.2.7]                           ││ │
│  │  │  └─ CoverageCalculator: 覆盖率统计 [v2.2.7]                              ││ │
│  │  └─────────────────────────────────────────────────────────────────────────┘│ │
│  │                                                                              │ │
│  │  ┌─────────────────────────────────────────────────────────────────────────┐│ │
│  │  │  【v2.2.7新增】Webhook模块                                               ││ │
│  │  │  ├─ WebhookConfig: Webhook配置 [v2.2.7]                                ││ │
│  │  │  ├─ EventListener: 事件监听 [v2.2.7]                                    ││ │
│  │  │  ├─ EventDispatcher: 事件分发 [v2.2.8]                                  ││ │
│  │  │  └─ StateNotifier: 状态通知 [v2.2.8]                                   ││ │
│  │  └─────────────────────────────────────────────────────────────────────────┘│ │
│  │                                                                              │ │
│  └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────────────┐ │
│  │                         状态存储层                                             │ │
│  │  ┌─────────────────────────────────────────────────────────────────────────┐│ │
│  │  │  state/notifications/                                                   ││ │
│  │  │  ├─ webhook_events/                                                    ││ │
│  │  │  └─ phase_changes/                                                     ││ │
│  │  └─────────────────────────────────────────────────────────────────────────┘│ │
│  └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 模块 | 技术/库 | 版本 | 选型依据 |
|------|---------|------|----------|
| CLI框架 | Click | >=8.0 | 现有技术栈 |
| 配置解析 | PyYAML | >=6.0 | 现有依赖 |
| HTTP监听 | http.server | 标准库 | 避免引入Flask依赖 |
| 签名验证 | hmac | 标准库 | GitHub webhook验证 |
| 覆盖率统计 | Markdown解析 | 标准库 | skill内容覆盖率 |

---

## 3. 核心模块设计

### 3.1 SkillTester (内容准确性验证)

#### 3.1.1 功能描述

**FR-TEST-001**: 自动化验证Skill内容与实际行为的一致性

#### 3.1.2 数据结构

```python
class ValidationLevel(Enum):
    ERROR = "ERROR"
    WARNING = "WARNING"
    INFO = "INFO"

@dataclass
class ValidationItem:
    code: str  # TEST-001 ~ TEST-006
    level: ValidationLevel
    message: str
    file_path: Optional[str] = None
    line_number: Optional[int] = None
    suggestion: Optional[str] = None

@dataclass
class ValidationResult:
    skill_id: str
    items: List[ValidationItem] = field(default_factory=list)
    passed: bool = True
```

#### 3.1.3 核心方法

```python
class SkillTester:
    SKILLS_DIR = "skills"
    
    def validate_outputs_exist(skill_id: str) -> ValidationResult:
        """验证outputs字段描述的文件是否存在"""
        
    def validate_triggers_match(skill_id: str) -> ValidationResult:
        """验证触发条件是否与content.md匹配"""
        
    def validate_sop_elements(skill_id: str) -> ValidationResult:
        """验证是否包含SOP四要素"""
        
    def run_all_tests(skill_id: Optional[str] = None) -> TestReport:
        """运行所有测试，返回测试报告"""
```

### 3.2 ReferenceValidator (引用关系验证)

```python
class ReferenceValidator:
    def validate_internal_links(skill_id: str) -> ValidationResult:
        """验证内部Markdown链接"""
        
    def validate_cross_skill_refs(skill_id: str) -> ValidationResult:
        """验证跨Skill引用"""
        
    def validate_anchors(skill_id: str) -> ValidationResult:
        """验证文档内锚点是否存在"""
```

### 3.3 CLIActionValidator (CLI命令验证)

```python
class CLIActionValidator:
    def extract_cli_commands(skill_id: str) -> List[str]:
        """从content.md提取CLI命令"""
        
    def validate_commands(commands: List[str]) -> ValidationResult:
        """验证命令是否存在"""
```

### 3.4 CoverageCalculator (覆盖率统计)

```python
class CoverageCalculator:
    def calculate_coverage(skill_id: str) -> float:
        """计算覆盖率"""
        
    def generate_report() -> CoverageReport:
        """生成覆盖率报告"""
        
    def check_threshold(threshold: float) -> bool:
        """检查是否达到阈值（默认95%）"""
```

### 3.5 WebhookConfig (Webhook配置)

```python
class WebhookConfig:
    def generate_config() -> WebhookConfig:
        """生成Webhook配置"""
        
    def generate_secret() -> str:
        """生成签名密钥"""
        
    def save_config(path: str) -> bool:
        """保存配置到 config/webhook.yaml """
```

### 3.6 EventListener (事件监听 + 崩溃恢复)

```python
class EventListener:
    def parse_github_payload(payload: dict) -> GitHubEvent:
        """解析GitHub webhook payload"""
        
    def parse_gitee_payload(payload: dict) -> GiteeEvent:
        """解析Gitee webhook payload"""
        
    def start_listening(port: int = 8080) -> None:
        """启动本地HTTP服务监听webhook"""
        
    def _crash_recovery(self):
        """崩溃恢复：自动重启 + 指数退避（最多3次）"""
```

### 3.7 EventDispatcher / StateNotifier (v2.2.8)

```python
class EventDispatcher:
    def dispatch(event: WebhookEvent) -> bool:
        """分发事件到对应Agent"""
        # push → Agent1
        # pull_request → Agent2
        
class StateNotifier:
    def detect_phase_change(old_state: dict, new_state: dict) -> Optional[PhaseChange]:
        """检测阶段变更"""
```

---

## 4. CLI 命令设计

### 4.1 skill test 命令

```bash
oc-collab skill test [--skill <id>] [--verbose] [--json]
```

### 4.2 skill coverage 命令

```bash
oc-collab skill coverage [--skill <id>] [--threshold <95>] [--json]
```

### 4.3 webhook init 命令

```bash
oc-collab webhook init [--force]
```

### 4.4 webhook start/stop 命令

```bash
oc-collab webhook start [--port 8080] [--no-recovery]
oc-collab webhook stop
```

---

## 5. 错误码设计

### Skill测试错误码

| 错误码 | 类型 | 说明 | 处理建议 |
|--------|------|------|----------|
| TEST-001 | ERROR | 文件不存在 | 检查文件路径配置 |
| TEST-002 | ERROR | 链接无效 | 修复Markdown链接 |
| TEST-003 | ERROR | CLI命令不存在 | 确认命令实现 |
| TEST-004 | WARNING | SOP四要素缺失 | 补充文档结构 |
| TEST-005 | WARNING | outputs字段缺失 | 添加或更新配置 |
| TEST-006 | INFO | 引用版本过旧 | 考虑更新引用 |

### Webhook错误码

| 错误码 | 类型 | 说明 | 处理建议 |
|--------|------|------|----------|
| WEB-001 | ERROR | Webhook密钥验证失败 | 检查secret配置 |
| WEB-002 | ERROR | 事件解析失败 | 检查payload格式 |
| WEB-003 | WARNING | 事件过滤跳过 | 确认filter配置 |
| WEB-004 | ERROR | 监听服务崩溃 | 检查端口，重启服务 |
| WEB-005 | WARNING | 重试次数耗尽 | 手动检查服务状态 |
| WEB-006 | INFO | 事件分发成功 | - |

### 退出码

| 退出码 | 含义 |
|--------|------|
| 0 | 成功 |
| 1 | 警告（部分失败） |
| 2 | 错误 |
| 3 | 致命错误 |

---

## 6. 崩溃恢复机制

### 恢复策略

| 策略 | 说明 |
|------|------|
| 自动重启 | 服务崩溃后自动重启 |
| 最大重试 | 最多重试3次 |
| 退避间隔 | 1min → 5min → 15min（指数退避） |
| 告警通知 | 重试耗尽后写入 state/notifications/ |

---

## 7. 测试策略

### 单元测试

| 模块 | 测试用例数 |
|------|----------|
| SkillTester | 10 |
| ReferenceValidator | 8 |
| CLIActionValidator | 6 |
| CoverageCalculator | 6 |
| WebhookConfig | 6 |
| EventListener | 8 |
| **合计** | **44** |

### 黑盒测试

| 命令 | 测试场景 |
|------|---------|
| skill test | 正常流程、异常流程、verbose输出 |
| skill coverage | 覆盖率计算、阈值检查 |
| webhook init | 配置生成、密钥生成 |
| webhook start/stop | 服务启动、崩溃恢复 |

---

## 8. 依赖关系

```
SkillTester ───┬──→ skills/*/skill.json
               │
ReferenceValidator ───→ skills/*/content.md
               │
CLIActionValidator ───→ src/cli/main.py
               │
CoverageCalculator ───→ SkillTester, SkillSlicer (v2.2.6)
               │
WebhookConfig ───┬──→ config/webhook.yaml
                 │
EventListener ───→ config/webhook.yaml
                 │
EventDispatcher ───→ EventListener
                 │
StateNotifier ───→ EventDispatcher, state/project_state.yaml
```

---

## 9. 工时记录 (v2.2.7)

| 任务 | 工时 | 状态 |
|------|------|------|
| SkillTester | 2h | 待开始 |
| ReferenceValidator | 2h | 待开始 |
| CLIActionValidator | 2h | 待开始 |
| CoverageCalculator | 4h | 待开始 |
| WebhookConfig | 4h | 待开始 |
| EventListener + 崩溃恢复 | 4h | 待开始 |
| CLI命令集成 | 3h | 待开始 |
| 单元测试 | 4h | 待开始 |
| **合计** | **21h** | |

---

## 10. 签署确认

### Agent 2 创建

| 角色 | 签署人 | 状态 | 日期 |
|------|--------|------|------|
| 开发负责人 | Agent 2 | ⏳ | 待签署 |

### Agent 1 验收

| 角色 | 签署人 | 状态 | 日期 |
|------|--------|------|------|
| 产品负责人 | Agent 1 | ⏳ | 待验收 |

---

**创建人**: Agent 2
**日期**: 2026-02-10
**状态**: DRAFT
